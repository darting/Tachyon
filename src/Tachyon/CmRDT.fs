namespace Tachyon.Collections.Replicable.Commutative

[<Sealed>]
type Counter = class end

[<Sealed>]
type BoundedCounter = class end

[<Sealed>]
type MVReg<'T> = class end

[<Sealed>]
type LWWReg<'T> = class end

[<Sealed>]
type GSet<'T> = class end

[<Sealed>]
type ORSet<'T> = class end

[<Sealed>]
type RGArray<'T> = class end